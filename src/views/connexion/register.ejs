<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AutoClean Pro - Connexion</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome pour l'icône d'œil -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <!-- Google Fonts: Dela Gothic One et Quicksand -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Dela+Gothic+One&family=Quicksand:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            height: 100vh;
            overflow: hidden;
            font-family: 'Quicksand', sans-serif;
        }
        
        h1, h2, h3, h4, h5, h6 {
            font-family: 'Dela Gothic One', cursive;
        }
        
        .logo {
            width: 50px;
            margin-bottom: 20px;
        }
        
        .login-container {
            height: 100vh;
        }
        
        .form-side {
            padding: 40px;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }
        
        .brand-side {
            background-color: #3B7097;
            color: white;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 40px;
        }
        
        .avatar {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            background-color: #75BDE0;
            margin: 30px 0 15px;
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        
        .avatar img {
            width: auto;
            height: auto;
        }
        
        .sign-in-btn {
            background-color: #4A8DB7;
            border: none;
            padding: 10px;
            font-weight: 600;
        }
        
        .sign-in-btn:hover {
            background-color: #3B7097;
        }
        
        .forgot-password {
            color: #4A8DB7;
            text-decoration: none;
            font-weight: 600;
        }
        
        .forgot-password:hover {
            color: #3B7097;
        }
        
        .sign-up {
            color: #4A8DB7;
            text-decoration: none;
            font-weight: 600;
        }
        
        .sign-up:hover {
            color: #3B7097;
        }
        
        .password-container {
            position: relative;
        }
        
        .password-toggle {
            position: absolute;
            right: 10px;
            top: 50%;
            transform: translateY(-50%);
            cursor: pointer;
            color: #4A8DB7;
            background: none;
            border: none;
        }
        
        h1 {
            font-size: 2rem;
            margin-bottom: 30px;
        }
        
        .brand-message {
            font-size: 2.2rem;
            max-width: 600px;
            font-family: 'Dela Gothic One', cursive;
        }
        
        .position-title {
            color: rgba(255, 255, 255, 0.8);
            font-weight: 500;
        }
        
        .form-check-input:checked {
            background-color: #4A8DB7;
            border-color: #4A8DB7;
        }
        
        .form-control:focus {
            border-color: #75BDE0;
            box-shadow: 0 0 0 0.25rem rgba(117, 189, 224, 0.25);
        }

        #teum {
            width: 100%;
        }
    </style>
</head>
<body>
    <div class="container-fluid p-0">
        <div class="row g-0 login-container">
            <!-- Côté formulaire -->
            <div class="col-md-6 brand-side">
                <div class="brand-message">
                    Allez viens on est bien
                </div>
                
                <div class="avatar">
                    <img id="teum" src="/images/Oim.jpg" alt="Teum">
                </div>
                
                <h3>Nahim</h3>
                <p class="position-title">Développeur web</p>
                </div>
                
            
            <!-- Côté marque/témoignage -->
            <div class="col-md-6 form-side">
                <div class="logo">
                    <svg viewBox="0 0 100 100" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <path d="M50 0L100 50L75 75L25 25L50 0Z" fill="#75BDE0"/>
                        <path d="M25 25L75 75L50 100L0 50L25 25Z" fill="#4A8DB7"/>
                    </svg>
                </div>
                
                <h1>Welcome tout court !</h1>
                
                <form>
                    <!-- Affichage des erreurs -->
                    <% if (typeof errors !== 'undefined' && errors.length > 0) { %>
                        <div class="alert alert-danger" role="alert">
                            <% errors.forEach(error => { %>
                                <p><%= error.msg %></p>
                            <% }) %>
                        </div>
                    <% } %>

                    <div class="mb-3">
                            <label for="nom" class="form-label">Nom</label>
                            <input type="text" class="form-control" id="nom" name="nom" value="<%= visiteur ? visiteur.nom : '' %>" required>
                    </div>

                    <div class="mb-3">
                        <label for="email" class="form-label">Email</label>
                        <input type="email" class="form-control" id="email" name="email" value="<%= visiteur ? visiteur.email : '' %>" required>
                    </div>
                    
                    <div class="mb-3">
                        <label for="password" class="form-label">Mot de passe</label>
                        <div class="password-container">
                            <input type="password" class="form-control" id="password" name="password" required>
                            <button type="button" class="password-toggle" onclick="togglePassword()">
                                <i class="fa-regular fa-eye"></i>
                            </button>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label for="password_confirm" class="form-label">Confirmer le mot de passe</label>
                        <input type="password" class="form-control" id="password_confirm" name="password_confirm" required>
                    </div>
                    
                    <button type="submit" class="btn btn-primary sign-in-btn w-100 mb-3">Valider</button>
                    
                    <div class="text-center mt-4">
                        <p>Vous êtes déjà un client fidèle ?</p>
                        <a href="login" class="sign-up">Se connecter</a>
                    </div>
                </form>
            
            </div>
        </div>
    </div>
    
    <!-- Bootstrap JS Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
        function togglePassword() {
            const passwordInput = document.getElementById('password');
            const icon = document.querySelector('.password-toggle i');
            
            if (passwordInput.type === 'password') {
                passwordInput.type = 'text';
                icon.classList.remove('fa-eye');
                icon.classList.add('fa-eye-slash');
            } else {
                passwordInput.type = 'password';
                icon.classList.remove('fa-eye-slash');
                icon.classList.add('fa-eye');
            }
        }
    </script>
</body>
<!--
    <form action="/user/register" method="post">
        <% if (typeof errors !== 'undefined' && errors.length > 0) { %>
            <div class="alert alert-danger" role="alert">
                <% errors.forEach(error => { %>
                    <p><%= error.msg %></p>
                <% }) %>
            </div>
        <% } %>
        <div class="form-row">
            <div class="form-group col-md-6">
                <label for="nom">Nom</label>
                <input type="text" class="form-control" id="nom" name="nom" value="<%= visiteur ? visiteur.nom : '' %>" required>
            </div>
            <div class="form-group col-md-6">
                <label for="prenom">Prénom</label>
                <input type="text" class="form-control" id="prenom" name="prenom" value="<%= visiteur ? visiteur.prenom : '' %>" required>
            </div>
        </div>
        <div class="form-group">
            <label for="email">E-mail</label>
            <input type="email" class="form-control" id="email" name="email" value="<%= visiteur ? visiteur.email : '' %>" required>
        </div>
        <div class="form-group">
            <label for="password">Mot de passe</label>
            <input type="password" class="form-control" id="password" name="password" required>
        </div>
        <div class="form-group">
            <label for="password_confirm">Confirmer le mot de passe</label>
            <input type="password" class="form-control" id="password_confirm" name="password_confirm" required>
        </div>
        Nouveau champ Téléphone
        <div class="form-group">
            <label for="telephone">Téléphone</label>
            <input type="tel" class="form-control" id="telephone" name="telephone" value="<%= visiteur ? visiteur.telephone : '' %>" required>
        </div>
         Nouveau champ Adresse 
        <div class="form-group">
            <label for="adresse">Adresse</label>
            <textarea class="form-control" id="adresse" name="adresse" rows="3" required><%= visiteur ? visiteur.adresse : '' %></textarea>
        </div>
        <div class="text-center">
            <button type="submit" class="btn btn-primary custom-btn">S'inscrire</button>
        </div>
        <p class="sousTitre">Vous avez déjà un compte ? <a href="/users/login">Connectez-vous !</a></p>
    </form>
    
</body>
</html>-->